package com.comp2042;

/**
 * Entry point for the application.
 * Used to launch the JavaFX application, ensuring proper runtime initialization
 * to avoid module-related issues when running as a JAR.
 */
public class Launcher {
    public static void main(String[] args) {
        Main.main(args);
    }
}
